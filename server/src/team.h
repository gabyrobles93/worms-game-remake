#ifndef __TEAM_H__
#define __TEAM_H__

#include <map>
#include "Worm.h"

class Team {
    private:
        size_t team_id;
        std::map<size_t, Worm*> & worms;

    public:
        Team(size_t, )
};

#endif