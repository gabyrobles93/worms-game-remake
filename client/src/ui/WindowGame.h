#ifndef __WINDOW_GAME_H__
#define __WINDOW_GAME_H__

#include <SDL2/SDL.h>

namespace View {
  class WindowGame {
    private:
      SDL_Window * window = NULL;
      SDL_Renderer * renderer = NULL;
      

    public:
  };
};

#endif
